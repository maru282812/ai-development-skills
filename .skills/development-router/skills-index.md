# Skill Index（自動生成 — 編集禁止）

このファイルは `node scripts/generate-skill-index.mjs` が各スキルの frontmatter から生成する。
手で編集しない。スキルを追加・変更したら再生成すること。

本文の場所: `C:\work\ai-development-skills\.skills\<name>\SKILL.md`

- **adversarial-review** — コード以外の判断物（料金・事業仮説・提供範囲・計画・見積り）を敵対視点で潰す汎用レッドチーム担当。複数の攻撃視点で前提の甘さ・反例・失敗シナリオを突き、対象は書き換えず指摘して止まる。
  - トリガー例: 「この判断を攻撃して」「レッドチームして」「敵対的にレビューして」 「この料金プランの穴を突いて」「前提の甘さを潰して」「反対意見を出して」 「この計画で失敗するとしたらどこ?」「投資家/契約先/競合の視点で潰しにかかって」。 コードのレビューは code-review / security-review、テスト台帳の対抗レビューは testmaster-a…
- **agent-planner** — 自発改善ループで次の一手を決める担当。git diff とテスターの検証レポートを読み、未達抽出→優先順位付け→次に実装すべき作業を1件に絞った「Claude Code用指示文」を出す。大規模実装はせず確認チェックリストで止まる。
- **agent-tester** — 実装直後の軽量検証担当。typecheck / test / lint・git diff・副作用/画面崩れ/DB/キャッシュ影響をスキャンして構造化した「検証レポート」を出す。軽微な修正のみ行い、ユーザー確認で止まる。
- **api-designer** — API設計担当。Next.js の Route Handler / Server Actions 前提で、エンドポイント・request/response・認証認可・エラー設計をまとめる。
  - トリガー例: 「API設計して」「エンドポイント考えて」「route handler 作って」 「request / response 考えて」「API仕様書を作って」 「Server Actions にするか API にするか」「認可チェックどこでやる?」 「APIのインターフェース決めて」「レスポンス形式どうする?」。 APIの新規設計・既存APIの拡張検討・API仕…
- **bug-investigator** — 不具合・エラーの原因特定担当。Next.js + Supabase 構成でエラーメッセージを解読し、再現条件の特定と原因の切り分けを行う（影響範囲調査は system-investigator）。
  - トリガー例: 「動かない」「エラー調査して」「原因調査して」「なぜ動かない?」 「500エラーが出る」「consoleにエラーが出てる」「PGRSTなんとかというエラー」 「Supabaseエラー」「buildは通るのに実行時エラーになる」「画面が真っ白」 「hydration error」「ログインできない」「データが表示されない」。 エラーメッセージの解読・再現条件の…
- **business-discovery** — Project Discovery の Phase5（Business Discovery）担当。事業として成立する前提・収益構造・顧客価値・優先ターゲット・成功条件を発見し、MVP時点の事業仮説を1本立てる。財務予測・PL作成はしない。
  - トリガー例: 「事業として成立するか整理したい」「ビジネスモデルを固めたい」「誰に何を売るのか決めたい」 「収益化するか無料運用かを判断したい」「事業観点のKPIを出して」「初期ユーザーをどう集めるか整理して」。 入力は [[scope-discovery]] / [[requirements-discovery]] / [[metrics-discovery]] の成果…
- **code-review** — 実装済みコードのレビュー担当。型・コンポーネント境界・認証認可・RLS・バリデーション・エラーハンドリング・既存機能への影響を確認する（セキュリティ深掘りは security-review）。
  - トリガー例: 「レビューして」「問題ないか見て」「バグがないか確認して」「実装漏れは?」 「設計的に大丈夫?」「このコードでいい?」「build は通るけど不安」 「セキュリティ的に危ない?」「この実装どう思う?」「直すところある?」。 実装後のコード確認・品質チェック・実装漏れ確認が含まれる依頼では このスキルを使用する。セキュリティ専門の深掘りは security-r…
- **contract-discovery** — Project Discovery の契約発見担当。「誰と・何を・どの条件で契約するか」を発見・整理する（提供範囲/対象外/SLA/返金/成果物権利/責任範囲など）。契約書は作らず条件は断定しない。
  - トリガー例: 「契約条件を整理したい」「提供範囲と対象外を決めたい」 「SLAを決めたい」「返金・キャンセル条件を固めたい」「成果物の権利を整理して」「責任範囲を明確にしたい」 「外注やNDAが要るか洗い出して」。AIは契約条件を断定しない（専門家確認を促す）。 実務上の依存順は requirements → data → contract → legal。入力は [[r…
- **data-discovery** — Project Discovery の Phase9（Data Discovery）担当。「何のデータを扱うサービスか」を確定する（扱う情報/取得元/更新責任者/保持期間/削除条件/個人情報/システム境界）。DB設計・テーブル・ER図はしない。
  - トリガー例: 「何のデータを扱うか整理して」「データの責任範囲を決めたい」「保存する情報を洗い出して」 「個人情報がどこにあるか出して」「外部システムとの境界を整理して」。 実装方法・テーブル・カラム・ER図・API・インフラは決めない（それは後工程）。 深掘りは [[discovery-planner]] で1テーマずつ、横断監査は [[discovery-audito…
- **data-flow-mapper** — データの流れの可視化担当。特定の表示値・テーブル・カラムがどこから来てどこへ保存されるかを追跡し図解する。
  - トリガー例: 「このデータどこから来てる?」「この値どこに保存される?」 「この画面の値は何テーブル?」「CRUD図にして」「どこで更新してる?」 「データの流れを整理して」「この項目の出どころは?」。 特定データの読み書き経路の追跡・可視化が主目的の依頼ではこのスキルを使用する。 改修箇所の特定や影響範囲の調査が主目的なら system-investigator、 不具…
- **db-designer** — DB設計担当。Supabase（PostgreSQL）前提でテーブル・カラム・制約・index・RLS を設計し、migration案まで作成する。
  - トリガー例: 「テーブル設計して」「DB設計して」「カラムを考えて」「ER図を考えて」 「Supabase のテーブルを作りたい」「RLS 前提で設計して」 「どのテーブルに持たせる?」「正規化した方がいい?」「スキーマを考えて」 「リレーションどうする?」。 新規テーブル設計・既存スキーマへのカラム追加検討・データモデリングが 含まれる依頼ではこのスキルを使用する。
- **design-reference-scout** — 参照サイト探索＋3軸提案担当。ギャラリー(MUUUUU等)を複数ページ×複数軸で実際に検索し、3案を軸(既存寄り/王道/尖り)で選び、各案にコンセプトと参照根拠を明記する。構成(骨格)とデザイン(見た目)を別軸で決める。
  - トリガー例: 「このサイトを作り直すとしたらどうする」「リデザインの提案をして」 「参考サイトを探して」「デザインの方向性を3案出して」「王道と尖った案を見たい」 「他社サイトを調べて提案して」「デザインコンセプトを立てて」「LPを新規で作りたい」 「このサイトのデザインを変えたい」「MUUUUUで探して」「ギャラリーから参考を集めて」。 「AIで綺麗に配置して作りました…
- **design-variant-preview** — デザイン案の見比べプレビュー担当。UI の見た目に迷ったとき、実装せずに3〜5案を同じデータで横並びにした1枚のプレビュー（Artifact / ローカルHTML）を出し、選定で止まる。
  - トリガー例: 「画面で見して」「画面で見せて」「デザイン案を出して」「何パターンか見せて」 「見た目を何案か作って」「どういう表現があるか見たい」「案を並べて見比べたい」 「ビジュアルで比較して」「モックを作って」「バッジの出し方を何通りか見せて」 「この一覧のレイアウト案が欲しい」「配色パターンを見比べたい」。 ユーザーが「見して」「見せて」「見たい」「何パターン」「何…
- **discovery-auditor** — Project Discovery の最終監査担当。decisions.md と全フェーズ成果物を横断し、決定と仮置きの混同・Phase間の矛盾・幽霊ドキュメント・未昇格仮置きの漏れ・MVPの線を検出してレポートを出し、サイクルを止める。
  - トリガー例: 「discovery を監査して」「抜け漏れ・矛盾を確認して」「実装に渡せる状態か見て」。
- **discovery-planner** — Project Discovery の意思決定プランナー担当。現在フェーズを仮置きで埋め切って完走させ、フェーズ末に「仮置き＋人間必須の要確認」を1枚の表で一括提示する。人しか決められない核は仮置きにしない。
  - トリガー例: 「次のフェーズを進めて」「discovery を1フェーズ進めて」「フェーズ末の確認を出して」 「次に決めるべきことを整理して」。[[project-discovery]] の各フェーズ進行で使う。
- **dispatch-pattern-builder** — 指示文ディスパッチ方式の機能を作る builder 担当。対象をDBに保存してID発行→コピー用スキル実行文→サブスク側で処理して書き戻す構成の実装指示と受け側スキルを設計する（API課金回避）。
  - トリガー例: 「この機能は指示文ディスパッチ方式で作って」 「ディスパッチ方式スキルで実装指示を作って」「サイトはDBとコピー用指示文だけ持たせて 処理はClaude Codeに投げたい」「LLM処理をAPI課金せずエージェント側でやる構成にして」 「studyId みたいなIDを発行してスキル実行文をコピーさせる形にして」 「testmaster みたいに対象を保存→C…
- **factory-bootstrap** — 製造ループ（Loop Engineering）基盤を対象プロジェクトへ薄く設置する担当。stack を検出して docs/VERIFY.md（検証レシピ）・.claude/loop.md（保守ループ既定）を生成し、CLAUDE.md に製造ループ節を追記する。既存ファイルは上書きしない。
  - トリガー例: 「ループエンジニアリングを導入して」「製造ループを入れて」「Factory を bootstrap して」 「このプロジェクトに loop 基盤を設置して」「製造工程を整えて」「VERIFY.md を作って」 「新規プロジェクトの製造ラインを整備して」。 実装の自走そのものは phase-runner、実行時検証の実施は verification-loop（…
- **feature-spec-writer** — 実装仕様書づくり担当。機能要望を、AIエージェント（Codex/Claude Code）にそのまま渡せる実装仕様書・指示文（目的・画面・API・DB・権限・受け入れ条件）に変換する。
  - トリガー例: 「指示文を作って」「仕様書にして」「Codexに投げたい」「実装指示にまとめて」 「この機能の仕様書を書いて」「AIに実装させる用の文章にして」。 目的・画面・API・DB・権限・受け入れ条件・実装指示を1つのドキュメントに まとめる依頼ではこのスキルを使用する。要件自体の整理(MVP分割・プラン設計)は saas-product-manager、実装手順の…
- **git-init-setup** — 新規プロジェクトの git/GitHub 初期化担当。既存 .git 退避→git init→初回コミット→GitHub private作成→remote連携→push を全自動で行う。安全ガードで既存リポジトリは保護する。
  - トリガー例: 「git作成」「gitを初期化して」「リポジトリ作り直して」「新規プロジェクトのgitを作って」 「GitHubにリポジトリ作って連携して」「git作成して」「このプロジェクトのgitを新規で作って」。 既存の .git を退避（.git.bak-<timestamp> へリネーム）→ git init（main）→ 初回コミット → GitHub に pr…
- **handover-package** — アプリ販売・譲渡の引き渡し資料づくり担当。環境変数一覧（値は書かない）・外部サービス名義切替・運用手順・法務掲載状況・開発用の後始末を handover/ 配下にまとめる。
  - トリガー例: 「引き渡し資料を作って」「販売用の引き継ぎパッケージを作って」 「譲渡ドキュメントをまとめて」「名義切替の手順を出して」「このアプリを売る準備をして」 「引き継ぎに何が要るか洗い出して」。 運営手順パネルのアプリ内埋め込みは [[ops-guide-embedder]]（本スキルはその剥がし手順を含む）、 運用業務の発見は [[operations-disc…
- **implementation-planner** — 実装計画づくり担当。Next.js + Supabase の機能開発で、目的・変更対象・実装順序・Phase分けを整理し、Codex/Claude Code にそのまま渡せる実装指示文を作る。
  - トリガー例: 「実装して」「Phase を作って」「実装指示文を考えて」「どの順番で作る?」 「どのファイルを作る?」「この機能を追加したい」「実装フェーズに分けて」 「Codex に投げる指示文を作って」「Claude Code に渡す実装指示を作って」 「実装計画を立てて」「タスクに分解して」。 新機能追加・既存機能改修の実装計画づくり、AIコーディングエージェント向…
- **integration-discovery** — Project Discovery の Phase10（Integration Discovery）担当。外部との接続点・依存先・連携責任を発見する（外部サービス/認証/通知/決済/データ入出力/失敗時対応）。API仕様・Webhook設計・技術選定はしない。
  - トリガー例: 「外部連携を整理したい」「使う外部サービスを洗い出して」「決済が必要か整理して」 「誰のアカウントで契約するか決めたい」「通知の送り先と失敗時対応を決めたい」「CSV取り込み・PDF出力の要否を整理して」 「外部API停止時の扱いを詰めたい」。入力は [[requirements-discovery]] / [[data-discovery]] / [[op…
- **kabeuchi** — idea-engine の事業アイデアを Claude Code 上で壁打ちし、確定した内容を Supabase の idea_reviews（8軸精査シート）へ直接パッチ反映するモード。 トリガー例: 「壁打ち」「壁打ちしたい」「〜について壁打ちして」「このアイデアを一緒に精査して」 「事業性を揉みたい」「アイデアの実現性を議論したい」。 ユーザーが「壁打ち」と言ったら、対象がアイデア・事業の検討である限りこのスキルに入る。 アプリ（…
  - トリガー例: 「壁打ち」「壁打ちしたい」「〜について壁打ちして」「このアイデアを一緒に精査して」 「事業性を揉みたい」「アイデアの実現性を議論したい」。 ユーザーが「壁打ち」と言ったら、対象がアイデア・事業の検討である限りこのスキルに入る。 アプリ（/ideas/[id]/review）の壁打ちチャットと同じデータ構造・同じテーブルに書くため、 ここでの壁打ち結果はアプリ…
- **legal-consistency-audit** — 法務文書群の横断整合性監査担当。規約・プラポリ・特商法・NDA・契約書・同意文言を突き合わせ、用語/責任範囲/データ取扱い/料金/準拠法/版管理の矛盾を検出する。適法性は断定せず指摘して止まる。
  - トリガー例: 「法務文書の矛盾を確認して」「規約とポリシーがズレてないか見て」 「利用規約とプライバシーポリシーを突き合わせて」「公開前に法務文書を横断チェックして」 「特商法と規約の記載が一致してるか監査して」「この5つの法務文書間で矛盾がないか監査して」。 何の法務成果物が必要かの発見は [[legal-discovery]]、掲載場所・版管理の運用は [[legal…
- **legal-discovery** — Project Discovery の Phase7（Legal Discovery）担当。公開・運営・課金・個人情報管理に「何の法務成果物が必要か」を発見する（利用規約/プラポリ/特商法/同意/返金・削除ポリシー等）。法的助言は断定しない。
  - トリガー例: 「必要な法務書類を洗い出して」「利用規約やプライバシー ポリシーが要るか整理して」「特商法表記が必要か確認して」「公開前に法務面の抜け漏れを潰したい」 「個人情報まわりで何を用意すべきか出して」。AIは法的助言を断定しない（専門家確認を促す）。 契約書そのものの発見は [[contract-discovery]]、PII・保持期間の前提は [[data-di…
- **legal-publication-manager** — 法務成果物の掲載・版管理の運用担当。必要文書を「どこに載せるか/どの雛形を使うか/どう更新管理するか」に変換し、掲載設計・成果物レジストリ・更新トリガーを生成する。適法性判断はしない。
  - トリガー例: 「法務文書をどこに載せるか整理して」「規約・ポリシーの雛形を用意して」「公開前チェックリストを作って」 「課金方式を変えたらどの法務文書を直すか決めたい」「法務成果物の状態を一覧管理したい」。 legal-discovery の判断を上書きしない（不明点は legal-open-questions と registry に同期）。 コードやUIは直接変更しな…
- **metrics-discovery** — Project Discovery の Phase11（Metrics / Success Discovery）担当。曖昧な成功状態を North Star / KPIツリー / 計測イベント / 計測手段に落とし、MVPで見るべき指標を3〜7個に絞る。ダッシュボード・SQL実装はしない。
  - トリガー例: 「成功条件を指標化したい」「KPIを決めたい」「何を計測すればいいか整理して」「North Star を決めたい」 「MVPで見るべき指標を絞りたい」「計測イベントを洗い出して」「KPIツリーを作って」。 入力は [[scope-discovery]] の scope/goals.md（成功状態・KPI候補）。出力は [[business-discovery…
- **migration-review** — migration SQL の本番適用前チェック担当。既存データ破壊・RLS漏れ・rollback可否を判定する。
  - トリガー例: 「migration 確認して」「SQL 確認して」「Supabase migration 大丈夫?」 「RLS が足りてる?」「policy 足りてる?」「既存データ壊れない?」 「rollback できる?」「本番適用して大丈夫?」「この ALTER TABLE 危なくない?」 「migration レビューして」。 migration ファイル・DDL…
- **nfr-discovery** — Project Discovery の Phase12（NFR Discovery）担当。機能要件だけでは漏れる非機能要件（性能/可用性/セキュリティ/運用性/保守性/バックアップ/監視/コスト）を発見し、MVP最低ラインと将来理想ラインに分けて明文化する。具体実装はしない。
  - トリガー例: 「非機能要件を整理したい」「性能・可用性の目標を決めたい」「バックアップとログ保持を決めたい」 「セキュリティ要件を明文化したい」「コスト上限を決めたい」「過剰設計か不足か点検して」。 入力は [[project-discovery]] / [[requirements-discovery]] / [[data-discovery]] / [[integra…
- **operations-discovery** — Project Discovery の Phase6（Operations Discovery）担当。継続運営に必要な運用実態（運営主体/権限/日次・週次運用/問い合わせ/異常時対応/運営KPI/手作業vs自動化）を発見する。機能・画面・インフラ設計はしない。
  - トリガー例: 「運営体制を整理したい」「誰が問い合わせ対応するか決めたい」「毎日必要な運用作業を洗い出して」 「返金・削除依頼の対応フローを決めたい」「運営KPIを出して」「MVPで手作業運用する範囲を確定したい」。 入力は [[business-discovery]] / [[scope-discovery]] / [[requirements-discovery]] …
- **ops-guide-embedder** — 運営者向け操作手順パネルの埋め込み担当。番号付き実行手順（場所タグ＋コピー可能なコマンド/実行文）を features/ops-guide/ に隔離埋め込みする。販売時はフォルダ削除＋マウント1箇所除去で一括剥がせる。
  - トリガー例: 「作業手順をサイトに書いて」「作業手順をサイトに入れて」「作業手順を画面に出して」 「操作手順をサイトに入れて」「使い方パネルを埋め込んで」「運営手順を画面に表示して」 「実行手順パネルを作って」「runbook をサイトに埋め込んで」「どこで何をするか画面に出して」 「ディスパッチの実行文を手順として並べて」「販売用に操作手順を後で一括削除できる形で入れて…
- **phase-runner** — 実装指示書(Phase 1..N)を、フェーズごとに fresh context のサブエージェントへ投げて自走実装するオーケストレータ。各フェーズは done ゲート(typecheck/test/build/verify。docs/VERIFY.md があればそれを優先)が緑→独立チェッカー(maker と別サブエージェント)が pass→Receipt 記録を経てから次へ進み、軽微な不具合は自動修正・重い問題(DB/認可/課金/破壊系/仕様の曖昧さ)は直さず周回停止して報告する。進捗は phase-status.md を唯一の真実として受け渡す。
  - トリガー例: 「実装を自走して」「フェーズを止めずに回して」「Phase1から10まで自動で実装して」 「実装指示書を最後まで回して」「AUTOで実装を進めて」「フェーズ自走」「実装フェーズを一気に回して」 「セッションを分けながら自動で実装して」。 実装計画/指示書そのものを作るのは implementation-planner / feature-spec-write…
- **project-discovery** — Project Discovery 全体のオーケストレーター担当。決めるべきこと（要件/UX/運営/収益/法務/リスク/データ/連携/指標/非機能/スコープ）を全部洗い出し、決定と仮置きを分離記録して実装可能な状態まで持っていく。各ドメインは *-discovery へ委譲する。
  - トリガー例: 「プロジェクトを立ち上げたい」「何を決めればいいか分からない」「実装前に全部詰めたい」 「要件だけじゃなく運営や法務も含めて穴を潰したい」「企画を実装可能な状態にして」。 各フェーズは [[discovery-planner]] が仮置きで完走させてフェーズ末にだけ一括確認し（フェーズ途中で止めない＝/loop 向き）、[[discovery-auditor…
- **project-kickoff** — ai-ideas 等で相談・確定したアイデアを、実装プロジェクトとして立ち上げるスキル。 ローカルフォルダの作成（既定 c:\work\<name>。既存があればそれを使う）→ 引継ぎ書の確保（既存を使う or 会話から生成）→ 参照資料のコピー → 引き継ぎ先の CLAUDE.md 整備 → git/GitHub 初期化（[[git-init-setup]] のスクリプトで git init〜GitHub private 作成〜初回 …
  - トリガー例: 「<名前> プロジェクト作成」「プロジェクト作成 <名前>」「<名前> に引き継いで」 「このアイデアを <名前> プロジェクトに渡して」「c:\work\<名前> に引き継いで」。 ユーザーが「プロジェクト作成」「引き継いで」と言ったら、直前までの会話がアイデア・構想の相談である限り このスキルに入る。フォルダが無ければ `c:\work\<name>` …
- **project-quality-tooling** — 新規案件の lint/format 初期セットアップ担当。スタックを検出して（TS→Biome / Swift→SwiftLint+SwiftFormat / Kotlin→ktlint）、設定ファイル・scripts・README まで一貫生成する。スタック越境はしない。
  - トリガー例: 「新規案件にLinter/Formatterを入れたい」「Biome入れて」「iOSのコード整形ツールを設定して」 「この案件にどの静的解析ツールが要るか判定して」「初期テンプレートにlint/format設定を入れて」 「Android Kotlinの整形ツールを整理して」「コード品質ツールを初期セットアップして」。 要件・スコープの発見はしない（それは …
- **prompt-architect** — アプリ組込み LLM プロンプトの設計・改善担当。system prompt / 分析 / インタビュー等のプロンプトを設計し、出力の安定性と品質を上げる。プロンプト保存機能の実装はしない。
  - トリガー例: 「プロンプト設計して」「system prompt 作って」「分析プロンプトを改善して」 「回答品質を上げたい」「AIの挙動を変えたい」「出力が安定しない」 「JSONで返ってこない」「プロンプトをレビューして」「few-shot 入れるべき?」。 AI機能(分析・インタビュー・要約・生成)のプロンプト作成・改善・構造化が 含まれる依頼ではこのスキルを使用す…
- **refactor-planner** — リファクタ計画づくり担当。既存挙動を壊さず、責務分離・共通化・ファイル分割を段階的に進める計画を作る（機能追加は伴わない）。
  - トリガー例: 「リファクタして」「共通化して」「責務分離して」「重複を減らして」 「service / repository に分けて」「ファイルが大きい」「保守しやすくして」 「既存挙動を壊さず整理して」「このコンポーネント分割して」 「コードを整理したい」。 既存コードの構造改善・整理・分割が含まれる依頼ではこのスキルを使用する。 機能追加を伴う場合は impleme…
- **requirements-discovery** — 実装前の要件洗い出し担当（Project Discovery の Phase2 も兼ねる）。業務フロー・利用者・状態・例外・通知・画面導線を段階式チェックリストで網羅質問し、後からの手戻りを減らす。一問一答はしない。
  - トリガー例: 「要件を洗い出して」「何が必要か整理して」「作る前に要件を固めたい」 「抜け漏れがないか確認して」「実装前に要件を詰めたい」「要件定義して」。 一問一答は禁止で、段階ごとの表でまとめて質問し、ユーザーが 「必要 / 不要 / 後で / 未定 / コメント」で返せる形式にする。 実装仕様書への変換は feature-spec-writer、MVP分割・Phas…
- **risk-discovery** — Project Discovery の Phase8（Risk Discovery）担当。MVP検証・事業継続・運営継続を妨げる主要リスクと前提条件を発見し、発生可能性×影響度で優先順位を付け、受容/軽減/後回し/情報不足に分類する。脆弱性診断はしない。
  - トリガー例: 「リスクを洗い出して」「何が起きたら事業が失敗するか整理して」「前提条件を明確にしたい」 「集客できなかった場合の代替を考えたい」「API停止時にサービス継続できるか整理して」「リスクの優先順位を付けて」。 入力は [[business-discovery]] / [[operations-discovery]] / [[integration-discov…
- **saas-product-manager** — SaaS 機能仕様の整理担当。ユーザー種別・導線・MVP・Phase分け・権限・運用要件を実装に落とせる形にまとめる（技術的な実装計画は implementation-planner）。
  - トリガー例: 「仕様を整理して」「SaaSとして考えて」「管理画面の仕様を考えて」 「ユーザー導線を考えて」「MVPに分けて」「優先順位を決めて」「Phase分けして」 「機能一覧にして」「要件をまとめて」「この機能どこまで作るべき?」。 機能の企画・仕様整理・スコープ決め・優先順位付けが含まれる依頼では このスキルを使用する。技術的な実装計画は implementati…
- **scope-discovery** — Project Discovery の最前段（Scope Discovery）担当。「そもそも何を/どこまで作るか・今回は作らないもの」を決めてプロジェクトの境界線を確定し、MVPの線を1本引く。画面/DB/API設計はしない。
  - トリガー例: 「何を作るか決めたい」「プロジェクトの範囲を固めたい」「今回どこまで作るか線を引きたい」 「対象外を明確にしたい」「要件定義の前に方向性を確定したい」「MVPの範囲を決めたい」。 画面設計・DB設計・API設計はしない。確定後 [[requirements-discovery]] へ引き渡す。
- **screen-design-architect** — 画面設計担当。要件成果物から必要画面・遷移・状態・UI構成・Stitch用プロンプトを生成し、Stitch出力やレビューで反復改善する。生成前に Screen Coverage Audit で網羅検査し、不足は要件へ差し戻す。
  - トリガー例: 「画面を設計して」「必要な画面を洗い出して」 「画面遷移を作って」「Stitch用のプロンプトを作って」「UI設計をレビューして直して」「画面の抜け漏れを確認して」。 このスキルは要件探索をしない（それは [[requirements-discovery]]）。要件→画面・導線・状態・UI仕様への変換、 Stitch連携、実装引き渡し（implementat…
- **security-review** — セキュリティ確認担当。Next.js + Supabase の RLS・service_role・認証認可・個人情報・ログ・Storage policy を対象に、情報漏洩・権限・認可の穴を点検する。
  - トリガー例: 「セキュリティ確認して」「RLS 大丈夫?」「service_role 危なくない?」 「個人情報大丈夫?」「認可漏れない?」「Supabase の権限確認して」 「API の認証確認して」「情報漏洩しない?」「他人のデータ見えない?」 「anon key で何ができる?」。 情報漏洩・権限・認可の不安、リリース前のセキュリティチェックが 含まれる依頼ではこ…
- **system-investigator** — 使用箇所・影響範囲の調査担当。既存システムで機能・項目・カラム・APIの使用箇所、影響範囲、改修候補箇所を特定する（エラー原因調査は bug-investigator）。
  - トリガー例: 「この項目(カラム/機能)は使われてる?」「どこを直せばいい?」「改修箇所を特定して」 「〇〇を変更したら影響範囲は?」「影響範囲を調べて」「検索キーワードは?」 「検索キーワードを洗い出して」「このAPIの呼び出し元は?」「このテーブルはどこから参照されてる?」 「関連ファイルを探して」「この機能の全体像を調査して」。 既存コードの使用箇所調査・影響範囲分…
- **tacit-knowledge-extractor** — 暗黙知の抽出・構造化担当。頭の中の業務ノウハウ・判断基準を対話で引き出し、再利用できる構造化ドキュメント（domain-knowledge/<topic>.md）に落とす。正しさの評価・意思決定はしない。
  - トリガー例: 「頭の中を言語化して」「暗黙知を文書化して」「ノウハウをまとめて」 「判断基準を言葉にしたい」「経験を資料に落として」「この業務の知見を構造化して」 「営業/接客/現場で自分がやってることを整理して」。 抽出した知識を要件に変換するのは [[requirements-discovery]]、事業仮説にするのは [[business-discovery]]、営…
- **test-planner** — テスト設計担当。Next.js + Supabase の機能に対するテスト観点・動作確認項目・受け入れ条件・リリース前チェックリストを作る。
  - トリガー例: 「テスト観点を考えて」「動作確認項目を作って」「受け入れ条件を作って」 「QA観点を出して」「E2Eテスト考えて」「手動確認手順を作って」 「regression 確認して」「テストケース作って」「チェックリスト作って」 「何を確認すればいい?」。 テスト設計・確認項目の洗い出し・リリース前チェックリスト作成が 含まれる依頼ではこのスキルを使用する。
- **testmaster-adversarial-review** — AI Testmaster の既存テスト台帳（テスト項目＝分母 と run の合否履歴）を「攻撃者・対抗レビュアー目線」で批判的に監査し、(1)抜けている観点 (2)甘い受け入れ条件（落ちようがない項目） (3)偽の合格（証拠なし合格・stale で現行を検証していない合格）を指摘して差し戻すスキル。対象アプリの実コードと突き合わせて、台帳が「現行バージョンの正しさを本当に保証しているか」を疑う。トリガー例「テスト項目を対抗レビューして…
  - トリガー例: 「テスト項目を対抗レビューして」「テスト台帳の抜けを突いて」「この合格は本物か検証して」「甘いテスト項目を洗い出して」「偽の合格を見つけて」「testmaster の項目をレッドチームして」「カバレッジ％は信用できる？」。テスト項目を新規生成するのではなく既存台帳を疑うのが仕事（生成は testmaster-test-plan、実行は testmaster-…
- **testmaster-full-plan** — AI Testmaster の「フル網羅」テスト項目生成。対象アプリのサーフェス(API/画面/DBテーブル/外部連携)をスクリプトで決定論的に列挙し、観点カタログ(正常/検証/認証/認可/RLS/異常/境界/UI/DB整合/領域固有/受け入れ)を機械的に当てて、サーフェス×観点マトリクスの空セルをゼロにするまで項目を作り込む。設計網羅率(分母の十分性)を測れる状態にして1回でimportする。トリガー例「テスト項目をフル網羅で作って」…
  - トリガー例: 「テスト項目をフル網羅で作って」「網羅的にテスト項目を洗い出して」「設計網羅率を出して」「抜けなくテスト項目を作って」「サーフェスを全部列挙してテスト項目にして」。部分的に領域だけ作り直すのは testmaster-test-plan。実行(合否記録)は testmaster-run-api / testmaster-run-screen。既存台帳を疑うのは…
- **testmaster-run-all** — AI Testmaster の1周（①テスト項目生成 → ②API合否記録 → ②画面/LIFF合否記録 → ③対抗レビュー → ④差し戻し反映）を、対象プロジェクトに対して停止せず一気通貫で自走させるオーケストレータ。対抗レビューの指摘がゼロになるまで自動で周回し、不合格が出たら「軽微(UI文言・明らかな小バグ)」は自動修正して再テスト、「重い(ロジック/DB/認可/課金/外部連携/破壊系)」は直さず不合格レポートに溜めて周回を止めて…
  - トリガー例: 「testmaster を一気に回して」「テスト作成から対抗レビューまで自走して」「<project> のテストを全部回して指摘ゼロまで自動で」「テストを止まらずに回して」「フル周回して」。単一フェーズだけ回したいときは testmaster-test-plan / testmaster-run-api / testmaster-run-screen / t…
- **testmaster-run-api** — AI Testmaster のテスト項目のうち「API契約系」を、対象アプリへ実際に HTTP リクエストを投げて検証し、合否を testmaster の Run Log に記録する。status コード・必須フィールド・認証(401/403)・入力検証(400)・異常(404/409)・境界などを直接叩いて確認する。トリガー例「testmaster のAPI項目をテストして記録して」「API契約テストを実行して」「契約テストを回して結…
  - トリガー例: 「testmaster のAPI項目をテストして記録して」「API契約テストを実行して」「契約テストを回して結果を入れて」。画面描画/ブラウザ操作が要る項目は対象外（それは testmaster-run-screen）。GET /api/projects/{id} で項目を取得し、自分が実行できる項目だけ実行、POST /api/projects/{id}/…
- **testmaster-run-screen** — AI Testmaster のテスト項目のうち「画面表示・遷移・フォーム操作系（LIFF含む）」を、ブラウザを操作して検証し、合否を testmaster の Run Log に記録する。Playwright（webapp-testing skill）で対象アプリを開き、画面描画・導線・状態・エラー画面・スクショで確認する。トリガー例「testmaster の画面項目をテストして記録して」「LIFF画面のテストを実行して」「画面表示テス…
  - トリガー例: 「testmaster の画面項目をテストして記録して」「LIFF画面のテストを実行して」「画面表示テストを回して結果を入れて」。純粋なAPI契約系は対象外（それは testmaster-run-api）。GET /api/projects/{id} で項目を取得し、自分が実行できる項目だけ実行、POST /api/projects/{id}/runs で記…
- **testmaster-test-plan** — AI Testmaster のテスト項目（分母）を、対象アプリの実コードを読んでエージェント自身が網羅生成し、testmaster へ書き戻す。API課金を使わず Claude Code / Codex のサブスクで完結する。トリガー例「testmaster のテスト項目を作って」「このアプリのテスト項目を洗い出して testmaster に入れて」「改修したのでテスト項目を更新して」。対象アプリのソースから feature/endpo…
  - トリガー例: 「testmaster のテスト項目を作って」「このアプリのテスト項目を洗い出して testmaster に入れて」「改修したのでテスト項目を更新して」。対象アプリのソースから feature/endpoint を発見し、領域ごとに具体テスト項目を列挙、各項目に派生元コードの fingerprint を付けて POST /api/projects/{id}/…
- **ui-ux-review** — UI/UX レビュー担当。管理画面・LIFF・スマホ画面の導線・フォーム・表示状態・文言を確認し改善案を出す。
  - トリガー例: 「UI見て」「UX改善して」「画面導線を考えて」「管理画面を使いやすくして」 「ボタン配置どうする?」「フォームを改善して」「スマホで使いやすくして」 「この画面わかりにくい」「文言を直して」「使いづらいんだけど」。 画面の使いやすさ・導線・見た目・文言の確認や改善が含まれる依頼では このスキルを使用する。
- **verification-loop** — 実装後の実行時検証ループ担当。docs/VERIFY.md のレシピに従い Gates（typecheck/lint/test/build）→ Runtime Verify（dev server起動・画面表示・主要操作・console error・スクショ）を回し、緑になるまで軽微修正を反復、重い問題は停止。証拠付き Receipt を出す。
  - トリガー例: 「実装を検証して」「動くか確認して」「verification loop を回して」「実機で確認して」 「画面まで含めて検証して」「検証レシピどおり確認して」「Receipt を残して」。 静的な軽量チェックと次指示の抽出は agent-tester / agent-planner、テスト台帳での網羅検証は testmaster 系、レシピの設置は fact…
